#include <QtMath>

namespace cnst
{
const float pi   = qAcos (-1);
const float pi_2 = pi / 2.0f;

constexpr float min_ui_float = 0.000001f;
constexpr float screen_in_pixels_2d = 512.0f;
constexpr float zoom_factor         = 5.0f;
} // namespace